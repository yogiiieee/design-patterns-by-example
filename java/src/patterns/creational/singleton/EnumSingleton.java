enum Singleton {
    INSTANCE;  // same as public static final Singleton INSTANCE = new Singleton();

    public void anyMethod() {
        // any logic here
    }
}

// Usage: Singleton.INSTANCE.anyMethod();
